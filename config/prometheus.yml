global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'sma-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # "backend" = nazwa serwisu w docker-compose
      - targets: ['backend:8080']
        labels:
          app: 'sma-backend'