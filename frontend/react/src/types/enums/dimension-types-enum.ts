export enum DimensionType {
    CONSOLE = 'console',
    SUSPENSION = 'suspension',
    GIRDERS = 'griders',
    BASIC_STRUCTURE = 'basicStructure',
}

export type DimensionTypeValue = `${DimensionType}`;